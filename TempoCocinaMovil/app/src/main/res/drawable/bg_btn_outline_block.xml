<?xml version="1.0" encoding="utf-8"?>
<!-- "Pausar todo": full-width pill with a 2dp teal outline. Not in the
     §9 catalog, which only has the half-width outline pair (§12). -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_pressed="true"><shape android:shape="rectangle">
        <solid android:color="@color/surface_alt"/>
        <stroke android:width="2dp" android:color="@color/primary"/>
        <corners android:radius="@dimen/radius_button"/></shape></item>
    <item android:state_focused="true"><shape android:shape="rectangle">
        <solid android:color="@color/surface"/>
        <stroke android:width="@dimen/focus_halo" android:color="@color/focus_color"/>
        <corners android:radius="@dimen/radius_button"/></shape></item>
    <item><shape android:shape="rectangle">
        <solid android:color="@color/surface"/>
        <stroke android:width="2dp" android:color="@color/primary"/>
        <corners android:radius="@dimen/radius_button"/></shape></item>
</selector>
