<?xml version="1.0" encoding="utf-8"?>
<!-- Active timer card. §12: the border is a soft orange, not the full
     #ED532A, and the exact value should come from Figma. Using the
     documented soft token until the MCP is reachable. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@color/surface"/>
    <stroke android:width="2dp" android:color="@color/timer_alert_border"/>
    <corners android:radius="@dimen/radius_card"/>
</shape>
